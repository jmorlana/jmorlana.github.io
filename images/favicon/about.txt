Favicon: the JM signature mark (images/logo.svg) inside a thin circle on the
page background. Source is favicon.svg; the PNGs and favicon.ico are rendered
from it. Regenerate them if the mark changes.
